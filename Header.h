#include <iostream>

// calculator.h
#pragma once

// Объявление функций для выполнения арифметических операций
int summ(int a, int b);
int min(int a, int b);
int multiply(int a, int b);
int division(int a, int b);

// Объявление функции для обработки входной строки
void mainfunk(std::string example);
